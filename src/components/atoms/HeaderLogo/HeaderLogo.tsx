import { CircleSlashed } from 'lucide-react';

const HeaderLogo = () => (
  <div className="px-8 py-6">
    <p className="flex items-center text-2xl font-semibold tracking-tight">
      <CircleSlashed className="mr-2" />
      Carbon Calc
    </p>
  </div>
);

export default HeaderLogo;
